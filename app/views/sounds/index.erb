<% unless @sounds.empty? %>
<table>
    <thead>
        <tr>
            <th>Title</th>
            <th>Soundfile</th>
        </tr>
    </thead>
    <tbody>
        <% @sounds.each do |sound| %>
        <tr>
            <td><a href="/sounds/<%= sound.id %>"><%= sound.title %></a></td>
            <td><a href="/<%= sound.public_file_path %>"><%= sound.soundfile %></a></td>
        </tr>
        <% end %>
    </tbody>
</table>
<% else %>
<h2>You don't have any sounds yet. <a href="/sounds/new">Upload a sound</a>.
<% end %>